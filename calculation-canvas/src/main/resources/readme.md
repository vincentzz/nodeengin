This is front-end project of graph computation, using JS, HTML and CSS. 

The graph is to compute the data flowing logic of each node.

Each node have multiple input data, and multiple output data. 
Each node have can have dependencies on data computated by other node. 

